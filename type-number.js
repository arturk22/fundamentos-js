// tipo number

const meuNumero = 3;

const numPrimeiro = 1;
const numSegundo = 3;

const operacaoMat = numPrimeiro / numSegundo;

console.log(operacaoMat);

//ponto Flutuante

const numeroFlutuante = 3.3;
const pontoFlutuanteSemZero = 0.3;

const novaOperacao = numPrimeiro + numeroFlutuante;

console.log(novaOperacao);